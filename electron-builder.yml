appId: com.xfq.froe.desktop
productName: Froe
directories:
  output: release
files:
  - out/**
  - package.json
  - resources/licenses/**
asar: true
mac:
  category: public.app-category.developer-tools
  icon: build/icon.icns
  target:
    - dmg
    - zip
  artifactName: froe-desktop-${version}-${arch}.${ext}
dmg:
  artifactName: froe-desktop-${version}-${arch}.${ext}
